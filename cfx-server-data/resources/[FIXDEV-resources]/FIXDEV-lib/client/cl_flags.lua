function SetPedFlag(object, flag, enabled)
    exports['FIXDEV-flags']:SetPedFlag(object, flag, enabled)
end

function HasPedFlag(object, flag)
    return exports['FIXDEV-flags']:HasPedFlag(object, flag)
end

function GetPedFlags(object)
    return exports['FIXDEV-flags']:GetPedFlags(object)
end

function SetPedFlags(object, flag)
    exports['FIXDEV-flags']:SetPedFlags(object, flag)
end

function SetVehicleFlag(vehicle, flag, enabled)
    exports['FIXDEV-flags']:SetVehicleFlag(vehicle, flag, enabled)
end

function HasVehicleFlag(vehicle, flag)
    return exports['FIXDEV-flags']:HasVehicleFlag(vehicle, flag)
end

function GetVehicleFlags(vehicle)
    return exports['FIXDEV-flags']:GetVehicleFlags(vehicle)
end

function SetVehicleFlags(object, flag)
    exports['FIXDEV-flags']:SetVehicleFlags(object, flag)
end

function SetObjectFlag(object, flag, enabled)
    exports['FIXDEV-flags']:SetObjectFlag(object, flag, enabled)
end

function HasObjectFlag(object, flag)
    return exports['FIXDEV-flags']:HasObjectFlag(object, flag)
end

function GetObjectFlags(object)
    return exports['FIXDEV-flags']:GetObjectFlags(object)
end

function SetObjectFlags(object, flag)
    exports['FIXDEV-flags']:SetObjectFlags(object, flag)
end